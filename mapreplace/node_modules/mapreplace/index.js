'use strict';

module.exports = function(str, map) {
	for (var key in map) {
		str = str.replace(new RegExp(key.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, '\\$&'), 'g'), function(word){
			return map[word];
		});
	}
	return str;
};
