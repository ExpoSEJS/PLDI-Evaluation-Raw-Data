import { XeroProto } from '../xero-proto';

export class Organisation extends XeroProto {
  constructor (config) {
    super(config, 'Organisation');
  }
  findAll (opts: IOptions = {}) {
    opts.singular = true;
    return super.get(`/${this.endpoint}`, opts);
  }
}