"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var xero_proto_1 = require('../xero-proto');
var Organisation = (function (_super) {
    __extends(Organisation, _super);
    function Organisation(config) {
        _super.call(this, config, 'Organisation');
    }
    Organisation.prototype.findAll = function (opts) {
        if (opts === void 0) { opts = {}; }
        opts.singular = true;
        return _super.prototype.get.call(this, "/" + this.endpoint, opts);
    };
    return Organisation;
}(xero_proto_1.XeroProto));
exports.Organisation = Organisation;
