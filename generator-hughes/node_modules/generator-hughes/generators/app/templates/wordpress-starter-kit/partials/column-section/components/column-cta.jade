- if ( get_row_layout() == 'column_link') :
	:php
		$button_text = get_sub_field('button_text');
		$button_link = get_sub_field('link');
	a.column-section__column--cta(href=$button_link) 
		button
			- echo $button_text
- endif;