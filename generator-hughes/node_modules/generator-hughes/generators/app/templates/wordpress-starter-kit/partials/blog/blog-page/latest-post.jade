:php
	$newestPost = new WP_Query();
	$newestPost->query('showposts=1');

- while ($newestPost->have_posts()) : $newestPost->the_post();
	:php
		$image = get_field('header_image');
		// vars
		$url = $image['url'];
		$title = $image['title'];
		$alt = $image['alt'];
		$caption = $image['caption'];
		// thumbnail
		$size = hero-images;
		$thumb = $image['sizes'][ $size ];
		$width = $image['sizes'][ $size . '-width' ];
		$height = $image['sizes'][ $size . '-height' ];

header.blog__latest-post
	.blog__latest-post__inner
		.blog__latest-post__content
			h1.blog__latest-post__content__title
				a(href!="<?php the_permalink() ?>" rel="bookmark")
					- the_title();
			.blog__latest-post__content__date
				i.fa.fa-clock-o.blog__latest-post__content__date__icon
					- the_date();
			h2.blog__latest-post__content__lede
				- the_field('lede')
			.blog__latest-post__content__read-more
				a(href!="<?php the_permalink() ?>" rel="bookmark")
					button.blog__latest-post__content__read-more__button 
						| Read More
						i.fa.fa-arrow-circle-right.blog__latest-post__content__read-more__button__icon
	.blog__latest-post__overlay
	.blog__latest-post__background
		img(src=$url)
- endwhile;