:php
 $headerImg = get_field('header_image');
 $lede = get_field('lede');
 $postTitle = get_the_title();

 // Field Vars
 $image = get_field('header_image');
 // Image Vars
 $url = $image['url'];
 $title = $image['title'];
 $alt = $image['alt'];
 $caption = $image['caption'];
 
 // thumbnail
 $size = 'homepage-hero';
 $thumb = $image['sizes'][ $size ];
 $width = $image['sizes'][ $size . '-width' ];
 $height = $image['sizes'][ $size . '-height' ];
		
section.single-post__hero
 .single-post__hero__inner
  .single-post__hero__content
   h1.single-post__hero__title
    - echo $postTitle;
   h2.single-post__hero__lede
    - echo $lede;
 .single-post__hero__overlay
 .single-post__hero__background--image
  img(src=$url alt=$alt)
