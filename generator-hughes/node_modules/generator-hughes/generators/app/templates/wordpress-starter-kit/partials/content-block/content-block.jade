- $bg_image = get_sub_field('background_image');
- $header_img = get_sub_field('header_image');

- if( get_row_layout() == 'content_block' ):
	section.content-block
		link(rel="stylesheet" media="all" href="<?php echo get_template_directory_uri() ?>/css/components/content-block.css")
		.content-block__inner
			- if( have_rows('content') ):
				- if ($bg_image) :
					.content-block__content.content-block__content--has-image
						- if($header_img) :
								// vars
								- $header_url = $header_img['url'];
								- $header_title = $header_img['title'];
								- $header_alt = $header_img['alt'];
								- $header_caption = $header_img['caption'];

								// thumbnail
								- $header_size = 'thumbnail';
								- $header_thumb = $header_img['sizes'][ $size ];
								- $header_width = $header_img['sizes'][ $size . '-width' ];
								- $header_height = $header_img['sizes'][ $size . '-height' ];
								.content-block__header-image
									img(src=$header_url)
							- endif;
						- while ( have_rows('content') ) : the_row();
							- get_template_part('partials/content-block/components/content-block-heading');
							- get_template_part('partials/content-block/components/content-block-main-content');
							- get_template_part('partials/content-block/components/call-to-action');

						- endwhile;
					- else:
						.content-block__content
							- if($header_img) :
								// vars
								- $header_url = $header_img['url'];
								- $header_title = $header_img['title'];
								- $header_alt = $header_img['alt'];
								- $header_caption = $header_img['caption'];

								// thumbnail
								- $header_size = 'thumbnail';
								- $header_thumb = $header_img['sizes'][ $size ];
								- $header_width = $header_img['sizes'][ $size . '-width' ];
								- $header_height = $header_img['sizes'][ $size . '-height' ];
								.content-block__header-image
									img(src=$header_url)
							- endif;
							- while ( have_rows('content') ) : the_row();	
								- get_template_part('partials/content-block/components/content-block-heading');
								- get_template_part('partials/content-block/components/content-block-main-content');
								- get_template_part('partials/content-block/components/call-to-action');
							- endwhile;
					-endif;
			- endif;
			
			

		- if( !empty($bg_image) ): 

			// vars
			- $bg_url = $bg_image['url'];
			- $bg_title = $bg_image['title'];
			- $bg_alt = $bg_image['alt'];
			- $bg_caption = $bg_image['caption'];

			// thumbnail
			- $bg_size = 'thumbnail';
			- $bg_thumb = $bg_image['sizes'][ $size ];
			- $bg_width = $bg_image['sizes'][ $size . '-width' ];
			- $bg_height = $bg_image['sizes'][ $size . '-height' ];
			.content-block__overlay
			.content-block__background
				img(src=$bg_url)
		- endif;
		
- endif;