:php
	/* ============== */
	/* MP4 Video Vars */
	/* ============== */
	$file_mp4 = get_sub_field('background_video--mp4');
	$url_mp4 = $file_mp4['url'];

	/* =============== */
	/* WEBM Video Vars */
	/* =============== */
	$file_webm = get_sub_field('background_video--webm');
	$url_webm = $file_webm['url'];
	
	/* ============== */
	/* OGG Video Vars */
	/* ============== */
	$file_ogg = get_sub_field('background_video--ogg');
	$url_ogg = $file_ogg['url'];
	
	$image = get_sub_field('background_video--poster');
	$url = $image['url'];
	$title = $image['title'];
	$alt = $image['alt'];
	$caption = $image['caption'];

	$size = 'thumbnail';
	$thumb = $image['sizes'][ $size ];
	$width = $image['sizes'][ $size . '-width' ];
	$height = $image['sizes'][ $size . '-height' ];

.page-hero__background--video
	| <video autoplay loop poster="<?php echo $url ?>">
	source(src=$url_mp4 type='video/mp4')
	source(src=$url_ogg type='video/ogg')
	source(src=$url_webm type='video/WebM')
	| </video>
	