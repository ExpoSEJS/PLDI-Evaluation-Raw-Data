:php
	// Field Vars
	$image = get_sub_field('background_image');
	// Image Vars
	$url = $image['url'];
	$title = $image['title'];
	$alt = $image['alt'];
	$caption = $image['caption'];

	// thumbnail
	$size = 'homepage-hero';
	$thumb = $image['sizes'][ $size ];
	$width = $image['sizes'][ $size . '-width' ];
	$height = $image['sizes'][ $size . '-height' ];
		
:php
	// Field Vars
	$image = get_sub_field('background_image');
	// Image Vars
	$url = $image['url'];
	$title = $image['title'];
	$alt = $image['alt'];
	$caption = $image['caption'];

	// thumbnail
	$size = 'hero-image';
	$thumb = $image['sizes'][ $size ];
	$width = $image['sizes'][ $size . '-width' ];
	$height = $image['sizes'][ $size . '-height' ];
	
	$thumbWebp = substr_replace($thumb , 'webp', strrpos($thumb , '.') +1);
		
picture.page-hero__background--image
	source(srcset=$thumbWebp)
	img(src=$thumb)