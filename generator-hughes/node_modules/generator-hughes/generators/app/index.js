'use strict';
var yeoman = require('yeoman-generator'),
    chalk = require('chalk'),
    yosay = require('yosay'),
    path = require('path'),
    fs = require('graceful-fs'),
    blender = require('yeoman-blender')(),
    mysql   = require('mysql');

// Add generator methods (order counts)
blender.methods([
//   require('./')
]);

// Add flavor directories (order counts)
blender.flavors([
  path.join(__dirname, 'mixins', 'intro'),
  path.join(__dirname, 'mixins', 'project'),
  path.join(__dirname, 'mixins', 'directories'),
  path.join(__dirname, 'mixins', 'css'),
  path.join(__dirname, 'mixins', 'database'),
  path.join(__dirname, 'mixins', 'wordpress'),
  path.join(__dirname, 'mixins', 'gulp'), 
  path.join(__dirname, 'mixins', 'website'),
  
  path.join(__dirname, 'mixins', 'save-package'), 
  path.join(__dirname, 'mixins', 'final-install')
]);

module.exports = yeoman.Base.extend(blender.blend());