'use strict';

var yeoman = require('yeoman-generator'),
    chalk = require('chalk'),
    yosay = require('yosay'),
		printTitle  = require('./../../helpers/printTitle'),
    fs = require('graceful-fs');

module.exports = {
	css: function() {
		
		this.log(printTitle('CSS Base'))
		
		var done = this.async();
		this.prompt([ {
      // Source directory
			type: "list",
      name: "cssBase",
      message: "Which CSS Base would you like to use?",
      choices: ['None', 'Normalize', 'Reset'],
			default: "Normalize"
    }], 

		function(props){
			this.props = props;
			// To access props later use this.props.someAnswer;
			 this.cssBase = this.props.cssBase,
	     
			done();
		}.bind(this))
	}};