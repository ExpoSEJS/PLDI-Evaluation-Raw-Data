/**
 * Create prompts for icon font
 */

'use strict';

var chalk       = require('chalk'),
    yosay       = require('yosay'),
    printTitle  = require('./../../helpers/printTitle');


module.exports = {
	intro: function() {
		var message = chalk.yellow.bold('Welcome to Hughes Generator ') + chalk.yellow('\'Cause automation is awesome');
 		this.log(yosay(message, {maxLength: 19}));
	}
}